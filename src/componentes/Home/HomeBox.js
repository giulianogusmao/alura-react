import React, { Component } from 'react';

import TituloPrincipal from '../TituloPrincipal';

export default class HomeBox extends Component {
    render() {
        return (
            <TituloPrincipal titulo="Bem-vindo ao sistema!" />
        );
    }
}
